# Pure Live 维护范围与问题处置策略

<!-- maintenance-policy-markers: android-first; windows-maintained; feature-requests-upstream; bug-provenance-required; semantic-change-ledger; evidence-layered; rollback-required; bugfix-android-release-default -->

本文件定义 `liuchuancong/pure_live` 维护分支的支持边界、Issue 分流、Bug 根因判定、上游同步和发布证据要求。它与 [上游同步审查策略](UPSTREAM_REVIEW_POLICY.md) 和 [构建资源策略](BUILD_POLICY.md) 一起构成本仓库后续工作的默认流程。

## 1. 仓库定位与维护范围

本仓库是 [liuchuancong/pure_live](https://github.com/liuchuancong/pure_live) 的高频维护分支，包含较多 Android、Windows、播放器、弹幕、界面、接口和发布流程定制。

| 平台 | 维护级别 | 说明 |
| --- | --- | --- |
| Android 手机/平板 | 主要维护、优先交付 | 当前使用和验证最多；多数修复、功能整合和安装包先在 Android 落地。 |
| Android TV | 随 Android 维护 | 共用 Android 主体代码；电视端特有交互需单独证据。 |
| Windows x64 | 主要维护 | 重点维护播放器、窗口、数据迁移、安装器、内存和高刷新率体验。 |
| Linux / macOS / iOS | 社区验证 | 保留源码和上游兼容性，缺少持续使用的对应设备，不承诺每轮安装包、时效或运行验证。带完整环境和回归证据的修复可按贡献流程审查。 |

本分支更新频繁、历史定制较多，仍可能出现较多回归、接口时效和设备兼容问题。若更看重低频变更或原项目行为，可使用[原项目](https://github.com/liuchuancong/pure_live)。每次 Release 必须如实标明本轮实际构建和验证的平台，不以“源码可编译”代替运行证据。

## 2. Issue 受理边界

本仓库 Issue 仅受理可复现的维护型问题，例如：

- 当前维护版本中的崩溃、黑屏、卡死、状态错乱或数据丢失；
- 播放、弹幕、平台接口、搜索、排行、安装、升级和配置迁移回归；
- Android 与 Windows 的性能、资源释放和交互一致性问题；
- 已有功能在上游同步后发生的兼容冲突。

新增功能、产品方向和全新平台适配统一提交到[原项目 Issue](https://github.com/liuchuancong/pure_live/issues/new/choose)。安全漏洞通过本仓库的 [Security Advisory](https://github.com/liuchuancong/pure_live/security/advisories/new) 私密提交。

Linux、macOS、iOS 等社区验证平台的问题可以保留为技术记录；处置结论会明确标为“社区证据”“待贡献者验证”或“已由上游覆盖”，不写成已完成本机验证。

## 3. 每个 Bug 的来源与根因判定

任何修复在动代码前都要完成 `bug-provenance-required` 分析。禁止把现象直接等同于根因，也不以反复增加刷新、延时、重建或重试掩盖生命周期问题。

### 3.1 最小输入

1. 记录当前版本、提交 SHA、平台、系统、播放器、直播平台和房间类型。
2. 写出最短复现步骤、期望、实际结果、最后正常版本和发生频率。
3. 将日志、截图、录屏、接口响应和配置迁移状态分层保存；清理 Cookie、令牌、账号和个人数据。
4. 对外部平台问题记录发生时间，因为直播接口、签名、CDN 和返回结构具有时效性。

### 3.2 来源分类

使用 merge base、冻结的上游提交和当前维护分支做三方比较，结论必须归入以下一类：

| 分类 | 判定 |
| --- | --- |
| `upstream-existing` | 同一根因已存在于冻结的上游代码或上游最新版。 |
| `fork-regression` | 维护分支新增或修改的代码单独引入回归。 |
| `integration-conflict` | 上游与维护分支各自单独成立，组合后因状态、默认值、生命周期或接口契约冲突而出错。 |
| `external-drift` | 平台接口、签名、协议、CDN、系统或第三方依赖发生变化。 |
| `environment-or-data` | 只由设备环境、旧配置、缓存、升级迁移或损坏数据触发。 |
| `not-reproduced` | 证据尚不足以定位；保留已核对范围、缺失证据和下一步，不宣称已经修复。 |

### 3.3 根因报告

每个 Bug 的提交、PR 或审计记录至少回答：

- 第一个错误状态在哪里产生，而不是最后在哪里显示；
- 触发它的调用链、事件时序、异步代次、订阅/Timer 生命周期或数据契约是什么；
- 上游、维护分支和合并后行为分别怎样；
- 为什么旧实现会失败，为什么新设计可覆盖复现和相邻状态；
- 选择直接复用上游、适配合并、兼容层或局部重写中的哪一种，以及未选方案的原因；
- 对普通竖屏、横屏、全屏、画中画、音频模式、弹幕、设置、升级数据和 Windows 窗口的影响；
- 回归测试、回滚点和剩余已知限制。

## 4. 上游代码同步

上游同步先审查、后合并。执行细节以 [UPSTREAM_REVIEW_POLICY.md](UPSTREAM_REVIEW_POLICY.md) 为准，并遵循以下固定顺序：

1. `fetch` 后冻结上游完整 40 位 SHA 和 merge base。
2. 分别阅读 `merge-base..upstream`、`merge-base..fork` 和潜在合并结果；原始 `git merge upstream/master` 不作为审查起点。
3. 审查全部入站提交、全部文件、删除、重命名、二进制、依赖、工作流和平台原生改动。
4. 建立 `semantic-change-ledger`，逐项写明：
   - 提交、文件、模块；
   - 上游修改目的和行为前后差异；
   - 对应 Issue/Bug、根因与修复方法；
   - 实现质量、边界条件、资源释放和更优方案；
   - 对本仓库定制功能和产品不变量的影响；
   - `accept`、`adapt`、`rewrite`、`drop` 或 `defer` 处置；
   - 所需测试、平台证据和回滚点。
5. 对会覆盖版本、更新源、签名、发布资产、设置默认值、播放器/弹幕生命周期、返回逻辑或本仓库 UI 的改动先做兼容设计。
6. 只有完成审计文档和静态门禁后才创建真实 merge；冲突解决后再次审查最终差异。

“编译通过”和“冲突已消除”都不是合并质量结论。对质量一般但意图正确的上游修改采用 `adapt` 或 `rewrite`；对会破坏维护分支且本轮证据不足的改动使用 `defer` 并记录恢复条件。

## 5. 原项目 Issue 的处理顺序

审查原项目 Issue 时，先处理**最新受支持版本**和**最新日期**的新报告，再按以下顺序细分：

1. `P0`：启动失败、普遍无法播放、数据破坏、安全或发布资产错误；
2. `P1`：稳定复现的黑屏、卡死、弹幕停更、主要平台接口失效、严重性能或升级回归；
3. `P2`：有替代路径的功能错误、布局和交互问题、单平台或单设备兼容问题；
4. `P3`：低频边界、文案、轻微视觉问题和证据不足的问题。

最新日期只决定首轮审查顺序，不覆盖严重程度、可复现性和影响范围。每条 Issue 都要映射到当前维护分支并标记为：`present`、`already-fixed`、`upstream-only`、`external-drift`、`not-reproduced`、`community-platform` 或 `deferred`。发现同类根因时合并设计和回归用例，避免按现象重复打补丁。

## 6. 影响面与验证矩阵

修复和上游同步至少核对受影响路径；正式发布按目标平台补齐证据：

- **Android**：首次进入、竖屏、横屏、全屏、系统画中画、应用小窗、音频/视频切换、前后台、侧滑返回、清晰度/线路、弹幕列表与覆盖层。
- **Windows**：普通窗口、全屏、小窗、窗口置顶、滚轮/高刷新率、播放器纹理、安装与便携升级、数据目录、长时间内存趋势。
- **公共逻辑**：启动刷新、关注状态、搜索/排行、接口超时与结构漂移、断网重连、房间切换、重复订阅、旧配置和备份恢复。
- **资源边界**：Timer、Stream、Worker、Controller、纹理、图片缓存和本地列表都要有创建、代次隔离、取消、释放与上限。

自动化优先覆盖状态机和数据契约。设备采样是额外证据；缺少某个平台设备时，如实记录证据层级，不把其他平台结果外推为该平台结论。

## 7. 完成、发布与回滚标准

1. 结论分为代码审查、自动化测试、本地构建、设备采样、外部接口探测，禁止混写。
2. 只有对应复现用例通过、相邻状态回归通过且产物来自目标提交时，才标记 `fixed`。
3. Release Notes 写明实际构建平台、ABI、签名、SHA256、已知限制、配置迁移和回滚方式。
4. 设置或数据结构变化必须给出旧安装默认值、迁移幂等性、备份恢复和降级影响。
5. 外部接口修复应保留类型容错、超时、错误分类、有限退避和可观测日志；禁止无限重试和静默吞错。
6. 性能优化以可重复基线比较 CPU、内存趋势、帧耗时、网络请求数和后台任务数量，不以主观“更流畅”作为唯一证据。
7. 不使用“全部 Bug 已修复”一类绝对结论；报告已覆盖范围、未覆盖平台和剩余风险。
8. 每个已完成的 Bug 修复批次默认递增补丁版本和 build，构建 Android `arm64-v8a` Release，并将最终源码、tag、正式签名 APK、构建元数据和校验文件同步到 `liuchuancong/pure_live`；多个同根因修复在同一任务内合并为一个版本。
9. 正式 APK 优先在本机构建，GitHub Actions 只承担 Secrets 短时签名和 Release 更新；Android、Windows 与其他平台始终分阶段串行，Bug 修复默认交付不会扩展为全平台构建。
10. 发布页面与 APK 核验通过后刷新 `assets/releases.json` 并推送索引提交。当前任务明确要求暂缓版本、构建或发布时，以该次要求为准并在报告中记录闭环停在哪一层。
