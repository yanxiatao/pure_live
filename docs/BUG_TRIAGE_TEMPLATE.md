# Bug 根因分析模板

> 复制本模板到对应 Issue、PR 或阶段审计文档。未取得的证据写“待验证”和所需条件，不省略字段。

## 基线与复现

- Issue / 报告：
- 当前维护分支 SHA：
- 冻结上游 SHA：
- merge base：
- 版本、平台、系统、设备、播放器：
- 直播平台、房间类型、发生时间：
- 最短复现步骤：
- 最后正常版本：
- 期望 / 实际：
- 发生频率与恢复条件：

## 来源判定

- 分类：`upstream-existing` / `fork-regression` / `integration-conflict` / `external-drift` / `environment-or-data` / `not-reproduced`
- 上游行为：
- 维护分支行为：
- 合并后或当前产物行为：
- 支撑证据：

## 根本原因

- 第一个错误状态：
- 调用链、事件时序或数据契约：
- 资源创建、代次隔离、取消与释放：
- 现象为何出现：
- 旧方案为何失效：

## 设计与实现

- 选择：直接复用 / `adapt` / 兼容层 / `rewrite`
- 方案：
- 未选方案及理由：
- 数据迁移与旧安装默认值：
- 外部接口容错、超时和错误分类：
- 回滚点：

## 影响矩阵

- Android：首次进入 / 竖屏 / 横屏 / 全屏 / 小窗 / 音频 / 前后台 / 返回
- 弹幕：连接 / 队列 / 列表 / 覆盖层 / 设置实时同步
- Windows：窗口 / 全屏 / 小窗 / 数据目录 / 内存趋势 / 高刷新率
- 公共逻辑：首页 / 关注 / 搜索 / 排行 / 设置 / 备份恢复 / 房间切换
- 未覆盖平台或模式：

## 证据与结论

- 代码审查：
- 确定性测试：
- Analyze：
- 目标平台构建：
- 设备或桌面运行采样：
- 接口探测与时间：
- 剩余风险：
- 结论：`fixed` / `already-fixed` / `deferred` / `not-reproduced`
